name: food_consulting_app
description: Food Consulting mobile app for international mission participants
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.0.0
  
  # State Management
  provider: ^6.1.1
  
  # Navigation
  go_router: ^12.1.3
  
  # Firebase (temporarily disabled for web compatibility)
  # firebase_core: ^2.24.2
  # firebase_messaging: ^14.7.10
  
  # UI Components
  cupertino_icons: ^1.0.6
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  flutter_markdown: ^0.6.18
  
  # Storage & Offline
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  
  # Utilities
  intl: ^0.19.0
  dio: ^5.4.0
  open_file: ^3.3.2
  share_plus: ^7.2.1
  add_2_calendar: ^3.0.1
  url_launcher: ^6.2.2
  geolocator: ^10.1.0
  permission_handler: ^11.1.0
  image_picker: ^1.0.4
  mask_text_input_formatter: ^2.5.0
  
  # Development
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  mockito: ^5.4.4
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
    
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Inter-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700
